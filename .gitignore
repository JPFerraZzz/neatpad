# ── Ambiente ─────────────────────────────
.env
.env.local
.env.*
!.env.example

# Dumps SQL locais (mantém database.sql versionado)
*.sql
!database.sql

# ── Credenciais / Segredos (NUNCA COMMITAR) ──────
# Configuração Firebase real (vive apenas no servidor)
assets/js/firebase-config.js
# Configuração do servidor com credenciais de BD e do projeto Firebase
config.php
# Backoffice NeatPad Studio (credenciais locais — nunca commitar)
np-gest/config.php

# ── Logs e temporários ──────────────────
*.log
/tmp/

# ── Sistema operativo ───────────────────
.DS_Store
Thumbs.db

# ── IDE ──────────────────────────────────
.vscode/
.idea/
*.swp

# ── Build / dependências ────────────────
/vendor/
/node_modules/
