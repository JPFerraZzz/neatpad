# Base de dados (Railway preenche automaticamente via MYSQL*)
DB_HOST=localhost
DB_PORT=3306
DB_USER=organizer
DB_PASS=organizer123
DB_NAME=ferraznest

# Firebase (opcional — está hardcoded em config.php como fallback)
FIREBASE_PROJECT_ID=ferraznest-54e93
